# Development Guide

## Supported agents

This repository supports following coding agents:

- Claude Code
- Codex
- GitHub Copilot
- Cursor
- OpenCode

## Release

## Naming rules

Each feature name should be in the following manner:

1. Start with `awh-`(abbreviation of `autoware harness`)
2. `<domain>`(like, `interface`, `perception`, etc.)
3. following sub domain parts, separated by '-'

## Agent vs Skill vs Command

See `./design.md`.

## Implementation

See `./format.md`.
